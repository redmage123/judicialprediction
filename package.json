{
  "name": "judicialpredict-root",
  "version": "0.0.0",
  "private": true,
  "description": "Project-root orchestration scripts. The web app lives in ./web with its own package.json.",
  "scripts": {
    "jp:seed-demo": "bash scripts/seed-demo.sh"
  }
}
