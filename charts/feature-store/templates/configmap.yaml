apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "feature-store.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "feature-store.labels" . | nindent 4 }}
data:
  # Placeholder — Sprint 2 will add RUST_LOG, MAX_CONNECTIONS, etc.
  {}
