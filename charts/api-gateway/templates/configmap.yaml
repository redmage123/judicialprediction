apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "api-gateway.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "api-gateway.labels" . | nindent 4 }}
# Sprint 2: populate non-secret runtime config here (RUST_LOG, feature flags, etc.)
# The Deployment already mounts this ConfigMap via envFrom so no template change is needed.
data: {}
