// Imperative shell: I/O orchestration with checkpointing (ADR-FP-001 Tier 3).

#[tokio::main]
async fn main() {
    tracing_subscriber::fmt::init();
    tracing::info!("ingest-fetcher placeholder — CourtListener + PACER ingestion (Sprint 2)");
}
