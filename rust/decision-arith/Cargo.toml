[package]
name = "decision-arith"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true }
rust_decimal = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
